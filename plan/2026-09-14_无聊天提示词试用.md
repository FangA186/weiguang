# [已完成] 无聊天提示词试用切换

> **状态**：已完成（本地切换与回归；未发布）
> **创建时间**：2026-09-14
> **完成时间**：2026-09-14

按用户要求注释语音planning_prompt注入，并在四个聊天入口过滤system/developer消息。保留会话历史、当前用户消息及图片视频；不更改OCR等任务提示词及TTS声音设置。语音改走普通文本分句，生成内部语音计划用于原有参数、音频缓存与回放，不要求模型生成NDJSON。自动场景不再由LLM选择，缺省使用chat。

验收：adaptive回归覆盖实际无system请求、文本碎片、无标点尾段、合成和回放；额度mock、后端编译通过，本地健康200。历史内容可能仍影响语气；建议新会话对比。未修改线上服务。

## 后续调整：恢复聊天规则，保留普通文本

最新用户调整：语音adaptive分支恢复NDJSON scene/text输出和PlanDecoder解析，普通文字聊天保持普通正文。planning_prompt增加structured开关，不再让两种格式要求冲突。保留多行JSON兼容和纯文本兜底。adaptive/额度回归通过，本地健康200，未发布。

用户要求恢复聊天提示词但去掉NDJSON。四个聊天入口已恢复统一日常口语system规则，删除提示中的JSON格式、scene输出及朗读计划要求；语音运行链路移除decoder分支，普通文本按标点分段。角色compiled_prompt未恢复，内部音频回放计划保留；自动场景不再由LLM输出。adaptive和额度回归通过，本地健康200，未发布。
