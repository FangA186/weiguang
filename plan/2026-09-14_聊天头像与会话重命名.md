# [未完成] 聊天头像与会话重命名

> **状态**：未完成
> **创建时间**：2026-09-14

聊天页点击AI头像选择图片，更新本会话头像；列表提供独立重命名按钮，标题最多64字。复用账号图片上传签名及已有会话title/avatar_object_key字段，通过新增PATCH更新，SQL以用户ID和会话ID共同限制。头像存储在账号私有前缀，更新会话不修改用户本人头像；无新表及Local Storage。保留旧图片对象，不自动删除。

验收：权限/标题/对象前缀mock，前端构建，本地页面检查。上线未包含在本轮操作中。

实现与验证：前端构建、后端编译、权限及边界mock通过；Chrome本地登录页面已显示独立重命名按钮及每条AI消息头像更换按钮。真实上传及刷新持久化未操作验收，保持未完成标记；未发布线上。

后续按用户反馈改为列表原位编辑：移除window.prompt及消息头像上传按钮；列表编辑展开标题输入和头像更换，保存标题/收起，头像选取后立即保存。Chrome已实测点击编辑出现输入框，无弹窗。正文padding未增加，实测16px 28.8px 40px，max-width 600px并居中；移除新增头像按钮包装。前端构建通过，未发布。
