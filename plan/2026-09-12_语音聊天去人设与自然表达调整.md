# [未完成] 语音聊天去人设与自然表达调整

> **状态**：未完成 (Pending)
> **创建时间**：2026-09-12

去掉语音生成中的角色 compiled_prompt，保留会话历史和用户选择的音色。使用日常口语规则，自动模式固定1.0语速，不自动插入情绪、笑声、叹息及逐句表演指令；手动声音参数保留。朗读计划按完整语意单元包含1到3句，降低逐短句重起调。

依据：百炼非实时语音合成官方文档支持Qwen-Audio-TTS指令和富语言标签；本次调整为应用层默认策略，不改变供应商协议。https://help.aliyun.com/zh/model-studio/non-realtime-tts-user-guide

验收：运行现有无外部调用的adaptive speech回归，核对人设不进入LLM请求、自动标签被移除、手动参数保留，检查后端编译及本地健康。听感需用户实际试听。

实施结果：以上源码已修改；test_adaptive_speech.py通过，后端编译通过，本地uvicorn --reload服务健康200。尚未发布；保留未完成状态等待主观听感验收。旧语音回放不重新生成，需发送新消息试听。

## 第二轮：恢复有节制的场景表达

设置面板同步：自动模式禁用不适用字段并说明生效范围，旧拟声auto映射为关闭展示；保留旧值和手动编辑。前端类型检查和构建通过。

用户反馈改善但缺少情感。替代首轮固定1.0策略：LLM输出chat/comfort/celebrate/playful/explain/serious场景，服务端映射0.96–1.04语速及自然语言声音指令。首个有效语意段确定整轮基调，后续段落不能跳变；未知场景回退chat。手动语速与情绪优先，自动模式不注入旧自由声音描述及广告/播报场景，避免矛盾。角色compiled_prompt仍不注入。

文案要求具体回应、真诚情感和适量口语，避免万能安慰、追问及编造共同经历；普通1到3句回答合为一个TTS单元，长回答按语意分段。自动笑声和叹息保持关闭。

验证：现有adaptive speech mock回归通过，新增覆盖安慰/喜悦映射、跨段稳定、非法场景回退、手动覆盖及旧声音描述隔离。后端编译通过。尚未做真实生成听感对比或线上发布，因此仍待试听验收。
