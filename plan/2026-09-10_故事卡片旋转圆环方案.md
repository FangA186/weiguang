# [未完成] 故事卡片旋转圆环方案

> **状态**：未完成 (Pending)
> **创建时间**：2026-09-10

> **实施情况**：圆环实现与构建检查完成，用户视觉验收待定。

## 范围

将现有六张 mock 故事卡排在同一 Three.js 场景的水平圆周上，使用略俯视的透视相机呈现前后层次。拖动旋转整个圆环，左右按钮/方向键切换前方卡片，支持自动旋转、暂停和当前卡片翻面及材质调节。

复用现有卡体、透明背面文字、珍珠银和主题配色；改用真实相机世界位置计算卡面反光，不引入独立扫光。保留既有 mock 数据和用户已修改的文案。单个 renderer、共享纹理，离屏暂停和卸载释放延续现有机制。

## 验证

- 圆周等距布局、环绕索引及视角更新检查。
- 前端构建、浏览器拖转/切换/翻面、窄屏边界与控制台检查。
- 视觉验收以用户实际观感为准；不接入投稿审核后端。

## 本轮结果

- 六张卡等距排列于半径 4.2 的圆周，单场景单 renderer，略俯视相机；卡片复用原模型，反光取真实相机世界位置。
- Chrome 拖动后当前卡与圆点联动，翻面控件正常切换；390px 无页面横向溢出，单画布且渲染 ready。窗口重测时防止负 dt 导致插值跳动。
- 圆周半径、等距间隔、索引环绕与材质状态测试通过，生产构建通过。手机完整触摸手势和最终视觉效果待进一步验收。
