# [已完成] 项目冗余代码静态审查

> **状态**：已完成 (Completed，仅审查，不含清理)
> **创建时间**：2026-09-13
> **完成时间**：2026-09-13

## 范围与证据

扫描backend、frontend/src、frontend-admin/src、scripts内97个Python/TS/JS/Vue/MJS文件；Python AST检查定义和导入，跨目录词引用检查，人工跟踪语音入口及路由。两个前端vue-tsc --noEmit --noUnusedLocals --noUnusedParameters通过。未扫描依赖包内部、发布包、生成文件、agency-agents第三方内容；没有执行数据库写入或删除任何源码。

词引用结果是静态证据，不保证外部脚本或动态加载不存在。保留公开HTTP端点、数据库迁移及仍在路由中的页面；不能把无前端调用等同于后端API可以删除。

## 用户指定符号

| 符号 | 真实作用 | 结论 |
|---|---|---|
| planning_prompt(profile) | app.py语音adaptive分支注入聊天口语规则与NDJSON输出格式 | 函数仍在使用，但profile形参未读取，可去掉形参并同步调用 |
| CONTROL_TAGS | VoiceProfile校验、clean_text清理、Qwen手动控制标签 | 使用中；CosyVoice文本清理也间接依赖 |
| EFFECT_TAGS | 拟声标签校验与clean_text清理、Qwen手动拟声 | 使用中，不能因CosyVoice不发送标签而整体删除 |
| EMOTIONS | Qwen及CosyVoice固定情绪中文映射 | 使用中 |
| DELIVERY | 场景合法性、Qwen自动语速/表达基调 | 使用中；CosyVoice语速后续会覆盖为用户rate |

移除角色compiled_prompt并不等于移除所有system提示；planning_prompt仍负责生成内容和结构化返回。去掉它会影响当前PlanDecoder协议。

## 可清理项，按可能减少代码量排序

1. delete: 前端API导出在扫描范围仅定义、无调用：frontend/src/api/index.ts中的chatAndTTS、clearChatMessages、fetchChatImport、deleteChatImport、fetchActivePersona、getPersonaDetail；frontend-admin/src/api/index.ts中的fetchAdminUserUsage；frontend/src/config/plans.ts中的quotaMetricLabel。可删客户端包装，不能连带删除后端端点。
2. shrink: backend/speech_plan.py resolve_segment先为Qwen计算rate/tag/effect/长instruction，CosyVoice分支随后覆盖。应提前按模型分支，仅共享校验与正文清理，无需新增类或工厂。
3. shrink: backend/providers/qwen_tts.py非流式/流式两套构建input逻辑重复，包括CosyVoice defaults、pitch/volume校验和指令截断。可提取一个同文件小函数，保持各自网络流程。
4. delete: backend/billing_store.py reserve_voice_slot、get_remaining仅定义无内部调用，是已有方法的薄包装。
5. delete: 未使用导入：backend/persona_distiller.py asyncio；backend/prompt_compiler.py json；backend/correction_handler.py json；backend/persona_store.py Path。
6. delete: frontend/src/views/ChatView.vue末尾if(streamError && !answerText)已不可达：上方if(!answerText.trim())已抛错。
7. delete: planning_prompt的profile形参；函数正文不使用此参数，函数本身不能删除。
8. shrink: resolve_segment从record读取tag/effect并检查类型后，无论manual还是auto都会重写，前面的读取可以去掉。

## 应保留或另行核验

- Qwen兼容分支：旧音色仍按target_model路由；默认系统音色仍用Qwen。
- /voice页面仍在Vue路由中，不能按页面看起来重复就删。
- PlanDecoder保留：流式文本需解析；近期多行JSON修复已有测试。
- 音色存在检查与就绪检查语义不同，不能直接合并成一个布尔值。
- frontend/src/lib/voiceInstruction.ts、ttsSettings.ts、backend/speech_plan.py均有声音描述逻辑；有相似内容，但分别承担预览、朗读和语音聊天，合并前需核验行为差异，不直接当死代码。
- 依赖清单未找到可有把握删除的包；release/dist/node_modules属于产物或依赖，不属于本轮源码清理。

## 验收总结

只完成静态审查。确定清理范围后可先删除无调用包装、导入、未使用形参及不可达判断，再单独简化模型分支；每步跑语音回归及两端构建。不删除存量音色、迁移、历史数据或公开API。

预计无调用包装等小项可减约60行，另有重复逻辑可缩减，依赖删除0项；未实施，非实测删除量。
