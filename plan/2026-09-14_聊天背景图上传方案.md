# [未完成] 聊天背景图上传

> **状态**：未完成 (Pending)
> **创建时间**：2026-09-14

聊天页右上角新增上传背景及恢复默认入口，账号级生效。复用现有头像图片签名上传（account-avatars/用户ID），仅允许5MB以内JPG/PNG/WebP。新增app_users.chat_background_object_key可空列，存储对象键；读取账号资料时返回临时签名URL。所有持久化走数据库，不使用localStorage。

新增PUT /api/auth/chat-background，object_key为null恢复默认；后端检查归属、对象大小和图片文件头后写库。同步路由由FastAPI在线程池运行，避免阻塞语音事件循环。参考：https://fastapi.tiangolo.com/async/ 。背景随账号删除纳入现有OSS清理，替换不立即删除旧对象，避免误删复用头像。

验收：归属/非法图片/超限/恢复默认回归、前端构建、浏览器入口；真实上传及刷新持久化使用用户选择的图片验证，未选图时明确保留待验收。

## 验证记录

- 前端生产构建通过。scripts/test_chat_background.py验证未登录、跨账号、非法路径、文件头、大小失败、OSS失败、恢复默认及用户限定SQL；全程mock无业务数据写入。会话头像回归亦通过。
- 本地数据库只读确认020迁移记录与chat_background_object_key字段存在；Chrome确认上传背景按钮可见并打开单文件选择器。
- 真实OSS上传、背景显示及刷新后持久化待用户选择图片后验收，因此方案状态仍为未完成。

## 2026-09-14 范围扩展

用户明确：全局指当前整个聊天页面，单独指右侧消息区域，不是全网站也不是逐会话。

- 保留chat_background_object_key作为整页背景；021迁移新增chat_panel_background_object_key。已有背景无需迁移。
- 同一PUT接口新增scope=global|panel，默认global兼容旧客户端。global单条UPDATE同时更新整页键并清空消息区键；panel只修改消息区键。文件校验失败不触发UPDATE。
- 前端增加上传全局背景、上传消息区背景，清除消息区背景只恢复透出全局，恢复默认清空两者。
- 全局上传清空单独背景、上传失败保留、单独清除保留全局、非法scope及用户限定SQL均已通过mock回归；生产构建通过；021迁移已只读确认；Chrome新页面确认原全局背景刷新回显及两个入口。
- 新的消息区实际上传、全局覆盖后的真实数据库回读待验收。原上传流程已由用户实际上传截图及本轮新页面回显补充验证，尚不等同于新范围的完整验收。

## 背景适配修正

全局与消息区图片均改为contain等比完整显示，比例不同产生的留白用主题底色填充。使用用户上传的两张图片在新页面确认持久化回显；桌面与390×844手机检查通过。真实全局覆盖清空单独背景的写入验收仍未执行。

### 2026-09-14 最新范围：仅消息区背景

用户要求移除全局背景。前端已移除全局显示、上传及整页恢复入口，仅保存scope=panel，清除后恢复默认深色。旧全局字段/API保留兼容，不再用于当前聊天页展示。构建和Chrome确认整页无背景、消息区原背景保留。

### 2026-09-14 用户取消背景功能

背景上传需求已取消，前端入口、上传调用及背景渲染均已移除，默认深色恢复；历史数据库字段、对象及后端接口保留兼容。构建和Chrome验收通过。此前上传验收项不再继续执行。
