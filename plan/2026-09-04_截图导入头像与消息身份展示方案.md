# [已完成] 截图导入头像与消息身份展示方案

> **状态**：已完成 (Completed)  
> **创建时间**：2026-09-04  
> **完成时间**：2026-09-04  
> **相关模块**：`backend/migrations/011_chat_import_display_meta.sql`、`backend/chat_import_store.py`、`backend/app.py`、`backend/app_data_store.py`、`frontend/src/components/ChatImportDialog.vue`、`frontend/src/views/ChatView.vue`

## 目标

1. 导入记录不再把左侧消息正文拼接为 `【对方】…`。
2. OCR 审核完成后可分别上传“左侧对方”和“右侧我”的聊天头像。
3. 头像与说话人名称随导入消息持久化，并在刷新历史记录后继续显示；私有 OSS 链接按读取时重新签发。

## 验证

- 前端生产构建与后端编译。
- 本地数据库应用迁移后，导入批次和聊天历史可读回头像元数据。
- 浏览器检查 OCR 审核页的左右头像上传入口与主聊天的左右头像渲染。

## 验收总结

- 本地数据库已应用 `011_chat_import_display_meta.sql`，并回读到导入左右头像和聊天导入元数据字段。
- 已在浏览器刷新现有历史，确认旧记录的 `【对方】` 前缀被隐藏；新导入不再写入该前缀。
- 前端生产构建与后端编译通过。头像真实上传需要在用户下一次 OCR 审核页中选择本地图片，未为验证而上传任何用户文件。
