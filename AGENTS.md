# 微光（Weiguang）AI 协作与开发规范（AGENTS.md）

本文档定义了所有 AI Agent（包括 Antigravity、Copilot 及其他自动化协作工具）在本项目中进行代码开发、方案设计、排障与维护时的核心规则与行为准则。

---

## 1. 方案归档与状态管理规范（强制执行）

1. **方案存储目录与命名规范**：
   - 所有新建立的需求方案、技术架构设计、重构方案或实施计划文档，**必须持久化保存在根目录的 `plan/` 目录下**。
   - **文件命名格式**：采用 **日期 + 中文名称** 格式，例如：`plan/2026-08-22_聊天记录持久化与历史回显方案.md`（标准格式：`YYYY-MM-DD_<中文方案名>.md`）。
   - 禁止仅将方案保留在临时上下文或临时会话工件中而不落盘到项目仓库。

2. **方案状态标记**：
   - 每个方案文档的**顶部一级标题（Title）必须明确标注当前状态**，格式统一为：
     - 未完成状态：`# [未完成] <方案名称>`
     - 已完成状态：`# [已完成] <方案名称>`
   - 在一级标题下方应包含状态元信息块，如：
     ```markdown
     > **状态**：未完成 (Pending) / 已完成 (Completed)
     > **创建时间**：YYYY-MM-DD
     > **完成时间**：YYYY-MM-DD (完成时填写)
     ```

3. **状态流转规则**：
   - **设计阶段**：创建方案时，初始状态设为 `[未完成]`。
   - **实施与交付阶段**：当该方案的所有功能编码、测试用例编写及验证验收全部通过后，**Agent 必须主动更新该方案文件**，将顶部标题与状态改为 `[已完成]`，并在文档末尾追加验收总结。

---

## 2. 项目进度跟踪与排障知识库规范（强制执行）

1. **统一进度文件维护**：
   - 根目录下设有 **`progress/`** 目录，作为项目进度跟踪与排障沉淀的专属目录。
   - 该目录下维护核心跟踪文件 **`progress/PROGRESS.md`** 以及配套的可视化页面 **`progress/index.html`**。
   - `progress/PROGRESS.md` 详细记录：
     - **已开发功能**（按模块、版本与功能描述归档）；
     - **已修复缺陷与功能**（记录缺陷背景与修复动作）；
     - **历史遇到问题与解决宝典**（包含问题现象、根因分析、具体修复方案、涉及代码文件与验证方式）。

2. **遇到问题“先查后写”闭环流程**：
   - **第一步（先查）**：每次在开发、测试或排障中遇到任何错误（如音频断续、爆音、WebSocket 1000/断开、模型一直 listen、数据库报错等），**必须首先查阅 `progress/PROGRESS.md`**，检索是否有类似问题的解决记录。若有，优先参照既有成熟方案排查修复。
   - **第二步（后写）**：如果该问题在 `progress/PROGRESS.md` 中尚未收录，或者参考历史方案后属于新衍生场景，在定位并彻底解决该问题后，**必须将当前问题的完整排查全过程（现象、根因、修复代码/配置、验证方法）追加沉淀到 `progress/PROGRESS.md`** 中，并同步更新可视化 HTML 数据，实现排障知识持续回流。

3. **官方资料优先核验规则（强制执行）**：
   - 每次需要确认问题、判断某项能力是否支持、选择技术方案或解释异常行为时，**必须先查询对应项目/厂商的官方资料**，包括官方文档、官方示例代码、官方源码、官方配置说明及官方 issue/变更记录。
   - 查询时应优先使用与当前版本和当前部署模式匹配的官方内容，并明确区分不同版本、协议和运行模式（例如 MiniCPM-o 的 Chat、Half-Duplex 与 Full-Duplex），不得把旧版 Demo 或其他模式的参数直接推断为当前模式可用。
   - 查到官方已有参数、推荐实现或已知限制时，优先按官方方案实现或调整；官方没有覆盖该问题时，才设计自有修复方案，并在交付说明中写明“官方依据、适用边界及自定义部分”。
   - 官方资料与本地代码/运行结果不一致时，必须以当前实际版本的源码、配置和运行验证为准，同时记录差异及原因，禁止仅凭记忆或搜索摘要下结论。

---

## 3. 核心架构与开发红线

1. **实时语音零阻塞原则**：
   - 实时双工语音（MiniCPM-o / 火山引擎）的 WebSocket 转发链路是核心资产，任何数据库写入、外部 API 调用、重型计算必须使用非阻塞异步机制（Goroutine / Channel），严禁阻塞 20ms 的音频数据帧传输。
2. **数据库迁移规范**：
   - 任何涉及数据表结构的新增或变更，必须在 `internal/auth/migrations/` 下按序号递增创建 SQL 迁移文件（如 `006_chat_history.sql`），利用 Go 的内嵌迁移机制自动升级。
3. **敏感凭据安全**：
   - 严禁在代码、注释、测试用例或文档中硬编码密码、Token、API Key、数据库真实密码等敏感信息。
4. **交接文档同步更新**：
   - 若修改了服务拓扑、关键端口、协议事件或核心业务流程，必须同步维护 `HANDOFF.md`。
5. **禁止使用 Local Storage 持久化数据（强制执行）**：
   - 严禁新增或继续依赖 `localStorage` 作为任何业务数据、用户数据或系统状态的存储来源，包括但不限于：账号资料、聊天记录、角色/音色设置、故事寄语、任务、额度、套餐订阅、业务单据与流程状态。
   - 所有需要跨刷新、跨设备或可追溯的数据，必须通过后端 API 写入数据库，并以数据库记录作为唯一权威数据源；前端只能展示后端返回的数据，不得用 `localStorage` 兜底伪造“已持久化”。
   - 现有 `localStorage` 用法一律视为待迁移技术债：禁止扩展字段、禁止新增 Key；后续修改相关功能时必须同步迁移至后端数据库，并完成旧数据迁移或兼容读取方案。
   - 可重新生成、丢失后不影响业务正确性的临时二进制缓存（例如 TTS 音频 Cache Storage）不属于权威业务数据，但必须设置容量/条数上限、支持清理，并在缓存失效时安全回源。
